# sec02 psql CRUD Basics

## 학습 목표
- 테이블 생성/삽입/조회/수정/삭제를 수행할 수 있다.
- WHERE, ORDER BY, LIMIT를 기본적으로 사용할 수 있다.

## 핵심 개념
- DDL: CREATE/ALTER/DROP
- DML: INSERT/UPDATE/DELETE
- Query 기본 패턴: SELECT ... FROM ... WHERE ...
