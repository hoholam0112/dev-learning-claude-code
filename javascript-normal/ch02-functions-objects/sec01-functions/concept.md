# 섹션 01: 함수 선언과 활용

> **난이도**: ⭐⭐ (2/5)
> **선수 지식**: 변수, 조건문, 반복문 (ch01)

---

## 학습 목표

이 섹션을 마치면 다음을 할 수 있습니다:

- 함수를 선언하고 호출할 수 있다
- 화살표 함수를 작성할 수 있다
- 매개변수와 반환값을 사용할 수 있다
- 기본 매개변수와 나머지 매개변수를 활용할 수 있다

---

## 핵심 개념

### 함수 선언

함수를 만드는 기본적인 방법입니다:

```javascript
// 함수 선언문
function greet(name) {
  return `안녕하세요, ${name}님!`;
}

console.log(greet("김철수")); // "안녕하세요, 김철수님!"
```

### 함수 표현식

변수에 함수를 할당하는 방법입니다:

```javascript
// 함수 표현식
const add = function (a, b) {
  return a + b;
};

console.log(add(3, 5)); // 8
```

### 화살표 함수

ES6에서 도입된 간결한 함수 작성법입니다:

```javascript
// 기본 화살표 함수
const multiply = (a, b) => {
  return a * b;
};

// 본문이 한 줄이면 중괄호와 return 생략 가능
const square = (x) => x * x;

// 매개변수가 하나면 괄호도 생략 가능
const double = x => x * 2;

console.log(multiply(3, 4)); // 12
console.log(square(5));      // 25
console.log(double(7));      // 14
```

> 💡 **React 연관**: React에서 컴포넌트와 이벤트 핸들러를 화살표 함수로 많이 작성합니다.
> `const Button = () => <button>클릭</button>`

### 매개변수와 반환값

```javascript
// 기본 매개변수 - 값이 전달되지 않으면 기본값 사용
function createUser(name, role = "회원") {
  return { name, role };
}

console.log(createUser("이영희"));          // { name: "이영희", role: "회원" }
console.log(createUser("박지민", "관리자")); // { name: "박지민", role: "관리자" }
```

### 나머지 매개변수

여러 개의 인수를 배열로 받습니다:

```javascript
function sum(...numbers) {
  let total = 0;
  for (const num of numbers) {
    total += num;
  }
  return total;
}

console.log(sum(1, 2, 3));       // 6
console.log(sum(10, 20, 30, 40)); // 100
```

### 콜백 함수

함수를 다른 함수의 인수로 전달합니다:

```javascript
function processData(data, callback) {
  const result = callback(data);
  console.log(`결과: ${result}`);
}

processData(5, (x) => x * 2);  // "결과: 10"
processData("hello", (s) => s.toUpperCase()); // "결과: HELLO"
```

> 💡 **React 연관**: React에서 이벤트 핸들러가 바로 콜백 함수입니다.
> `<button onClick={() => setCount(count + 1)}>클릭</button>`

---

## 코드로 이해하기

### 예제: 다양한 함수 패턴

```javascript
// 인사말 생성기
const createGreeting = (name, time = "오전") => {
  const greetings = {
    오전: "좋은 아침이에요",
    오후: "좋은 오후예요",
    저녁: "좋은 저녁이에요",
  };
  return `${greetings[time] || "안녕하세요"}, ${name}님!`;
};

console.log(createGreeting("철수"));         // "좋은 아침이에요, 철수님!"
console.log(createGreeting("영희", "저녁")); // "좋은 저녁이에요, 영희님!"
```

**실행 방법**:
```bash
node exercise.js
```

---

## 주의 사항

- ⚠️ 화살표 함수에서 객체를 바로 반환할 때는 괄호로 감싸야 합니다: `() => ({ key: value })`
- ⚠️ 함수 선언문은 호이스팅되지만, 함수 표현식과 화살표 함수는 호이스팅되지 않습니다
- 💡 함수는 하나의 일만 하도록 작성하면 유지보수가 쉬워집니다

---

## 정리

| 개념 | 설명 | 예제 |
|------|------|------|
| 함수 선언문 | 기본적인 함수 정의 | `function add(a, b) { return a + b; }` |
| 화살표 함수 | 간결한 함수 표현 | `const add = (a, b) => a + b;` |
| 기본 매개변수 | 기본값이 있는 매개변수 | `function f(x = 10) { }` |
| 나머지 매개변수 | 가변 인수를 배열로 | `function f(...args) { }` |
| 콜백 함수 | 인수로 전달되는 함수 | `arr.map(x => x * 2)` |

---

## 다음 단계

- ✅ `exercise.md`의 연습 문제를 풀어보세요.
- 📖 다음 섹션: **sec02-objects-arrays: 객체와 배열**
- 🔗 참고 자료: [MDN - 함수](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions)
