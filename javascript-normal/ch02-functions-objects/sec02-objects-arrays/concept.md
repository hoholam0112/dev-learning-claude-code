# 섹션 02: 객체와 배열

> **난이도**: ⭐⭐ (2/5)
> **선수 지식**: 변수, 함수 (ch01, sec01)

---

## 학습 목표

이 섹션을 마치면 다음을 할 수 있습니다:

- 객체를 생성하고 속성에 접근할 수 있다
- 배열을 생성하고 요소를 추가/삭제할 수 있다
- 객체와 배열을 중첩하여 복잡한 데이터를 표현할 수 있다

---

## 핵심 개념

### 객체 (Object)

객체는 **키-값 쌍**으로 데이터를 저장합니다:

```javascript
const user = {
  name: "김철수",
  age: 28,
  email: "kim@example.com",
  isActive: true,
};

// 속성 접근 — 점 표기법
console.log(user.name); // "김철수"

// 속성 접근 — 대괄호 표기법
console.log(user["email"]); // "kim@example.com"

// 속성 추가
user.phone = "010-1234-5678";

// 속성 수정
user.age = 29;
```

> 💡 **React 연관**: React의 props는 객체입니다. `<User name="김철수" age={28} />`에서 props는 `{ name: "김철수", age: 28 }`입니다.

### 객체 단축 속성

변수 이름과 키 이름이 같으면 줄여 쓸 수 있습니다:

```javascript
const name = "이영희";
const age = 25;

// 단축 속성 — 키와 값의 변수명이 같으면 생략 가능
const user = { name, age };
// 위는 { name: name, age: age }와 동일

console.log(user); // { name: "이영희", age: 25 }
```

### 배열 (Array)

배열은 **순서가 있는** 데이터의 집합입니다:

```javascript
const fruits = ["사과", "바나나", "체리"];

// 인덱스로 접근 (0부터 시작)
console.log(fruits[0]); // "사과"
console.log(fruits[2]); // "체리"

// 길이 확인
console.log(fruits.length); // 3

// 요소 추가
fruits.push("딸기");     // 끝에 추가
console.log(fruits);      // ["사과", "바나나", "체리", "딸기"]

// 요소 삭제
fruits.pop();             // 마지막 요소 삭제
console.log(fruits);      // ["사과", "바나나", "체리"]

// 배열 확인
console.log(Array.isArray(fruits)); // true
```

### 중첩 데이터

객체 안에 배열, 배열 안에 객체를 넣을 수 있습니다:

```javascript
const classroom = {
  name: "3학년 1반",
  students: [
    { name: "김철수", score: 90 },
    { name: "이영희", score: 85 },
    { name: "박지민", score: 95 },
  ],
};

// 중첩 데이터 접근
console.log(classroom.students[0].name); // "김철수"
console.log(classroom.students[2].score); // 95
```

> 💡 **React 연관**: API에서 받아오는 데이터가 대부분 이런 중첩 구조입니다. 이 데이터를 화면에 렌더링하는 것이 React의 핵심 역할입니다.

### 스프레드 연산자로 복사

```javascript
// 배열 복사
const original = [1, 2, 3];
const copy = [...original];
const extended = [...original, 4, 5]; // [1, 2, 3, 4, 5]

// 객체 복사
const user = { name: "김철수", age: 28 };
const updatedUser = { ...user, age: 29 }; // age만 변경된 새 객체
console.log(updatedUser); // { name: "김철수", age: 29 }
```

> 💡 **React 연관**: React에서 상태를 업데이트할 때 스프레드 연산자를 자주 사용합니다. `setState({ ...state, count: state.count + 1 })`

---

## 코드로 이해하기

### 예제: 학생 성적 관리

```javascript
const students = [
  { name: "김철수", scores: [90, 85, 92] },
  { name: "이영희", scores: [78, 92, 88] },
];

for (const student of students) {
  let total = 0;
  for (const score of student.scores) {
    total += score;
  }
  const average = total / student.scores.length;
  console.log(`${student.name}: 평균 ${average.toFixed(1)}점`);
}
```

**실행 방법**:
```bash
node exercise.js
```

---

## 주의 사항

- ⚠️ `const`로 선언한 객체/배열의 **내용**은 변경할 수 있습니다. 참조 자체만 변경 불가합니다.
- ⚠️ 배열 인덱스는 0부터 시작합니다. `arr[arr.length]`는 `undefined`입니다.
- 💡 객체/배열을 복사할 때 `=`는 참조 복사입니다. 독립적인 복사는 스프레드 연산자를 사용하세요.

---

## 정리

| 개념 | 설명 | 예제 |
|------|------|------|
| 객체 리터럴 | 키-값 쌍의 데이터 | `{ name: "철수", age: 28 }` |
| 점 표기법 | 속성 접근 | `obj.name` |
| 배열 | 순서 있는 데이터 | `["a", "b", "c"]` |
| `push`/`pop` | 배열 끝에 추가/삭제 | `arr.push(4)` |
| 스프레드 | 복사/확장 | `{ ...obj, key: value }` |

---

## 다음 단계

- ✅ `exercise.md`의 연습 문제를 풀어보세요.
- 📖 다음 섹션: **sec03-array-methods: 배열 메서드**
- 🔗 참고 자료: [MDN - 객체로 작업하기](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Working_with_objects)
