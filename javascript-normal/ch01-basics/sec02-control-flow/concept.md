# 섹션 02: 조건문과 반복문

> **난이도**: ⭐ (1/5)
> **선수 지식**: 변수와 데이터 타입 (sec01)

---

## 학습 목표

이 섹션을 마치면 다음을 할 수 있습니다:

- `if/else`로 조건에 따라 다른 코드를 실행할 수 있다
- 삼항 연산자로 간단한 조건을 표현할 수 있다
- `for`와 `while` 반복문을 사용할 수 있다
- `switch`문으로 여러 조건을 처리할 수 있다

---

## 핵심 개념

### if/else 문

조건에 따라 다른 코드를 실행합니다:

```javascript
const score = 85;

if (score >= 90) {
  console.log("A 등급");
} else if (score >= 80) {
  console.log("B 등급");
} else if (score >= 70) {
  console.log("C 등급");
} else {
  console.log("D 등급");
}
// 출력: "B 등급"
```

### 비교 연산자

| 연산자 | 의미 | 예시 |
|--------|------|------|
| `===` | 값과 타입이 같음 (엄격 비교) | `5 === 5` → `true` |
| `!==` | 값 또는 타입이 다름 | `5 !== "5"` → `true` |
| `>` | 크다 | `10 > 5` → `true` |
| `<` | 작다 | `3 < 7` → `true` |
| `>=` | 크거나 같다 | `5 >= 5` → `true` |
| `<=` | 작거나 같다 | `3 <= 2` → `false` |

> ⚠️ `==` (느슨한 비교) 대신 항상 `===` (엄격 비교)를 사용하세요. `==`는 타입 변환을 하기 때문에 예상치 못한 결과가 나올 수 있습니다.

### 논리 연산자

```javascript
const age = 25;
const hasLicense = true;

// AND (&&) - 모든 조건이 true여야 true
if (age >= 18 && hasLicense) {
  console.log("운전 가능");
}

// OR (||) - 하나라도 true이면 true
const isWeekend = true;
const isHoliday = false;
if (isWeekend || isHoliday) {
  console.log("쉬는 날");
}

// NOT (!) - true ↔ false 반전
const isLoggedIn = false;
if (!isLoggedIn) {
  console.log("로그인이 필요합니다");
}
```

### 삼항 연산자

간단한 조건은 삼항 연산자로 한 줄로 표현할 수 있습니다:

```javascript
const age = 20;
const status = age >= 18 ? "성인" : "미성년자";
console.log(status); // "성인"
```

> 💡 **React 연관**: React에서 조건부 렌더링할 때 삼항 연산자를 자주 사용합니다.
> `{isLoggedIn ? <Dashboard /> : <Login />}`

### for 반복문

지정된 횟수만큼 코드를 반복합니다:

```javascript
// 1부터 5까지 출력
for (let i = 1; i <= 5; i++) {
  console.log(i);
}

// 배열 순회
const fruits = ["사과", "바나나", "체리"];
for (let i = 0; i < fruits.length; i++) {
  console.log(fruits[i]);
}
```

### for...of 반복문

배열의 각 요소를 순회하는 더 깔끔한 방법입니다:

```javascript
const fruits = ["사과", "바나나", "체리"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

### while 반복문

조건이 true인 동안 계속 반복합니다:

```javascript
let count = 0;
while (count < 3) {
  console.log(`카운트: ${count}`);
  count++;
}
// 출력: 카운트: 0, 카운트: 1, 카운트: 2
```

### switch 문

여러 값을 비교할 때 `if/else`보다 깔끔할 수 있습니다:

```javascript
const day = "월";

switch (day) {
  case "월":
  case "화":
  case "수":
  case "목":
  case "금":
    console.log("평일");
    break;
  case "토":
  case "일":
    console.log("주말");
    break;
  default:
    console.log("알 수 없는 요일");
}
```

---

## 코드로 이해하기

### 예제: 성적 평가 시스템

```javascript
function evaluateScore(score) {
  if (score < 0 || score > 100) {
    return "유효하지 않은 점수";
  }

  if (score >= 90) return "A";
  if (score >= 80) return "B";
  if (score >= 70) return "C";
  if (score >= 60) return "D";
  return "F";
}

const scores = [95, 82, 67, 73, 55];
for (const score of scores) {
  console.log(`${score}점: ${evaluateScore(score)}`);
}
```

**실행 방법**:
```bash
node exercise.js
```

---

## 주의 사항

- ⚠️ `===`와 `==`를 혼동하지 마세요. 항상 `===`을 사용하세요.
- ⚠️ `switch`문에서 `break`를 빠뜨리면 다음 case까지 실행됩니다 (fall-through).
- ⚠️ 무한 루프에 주의하세요. `while` 반복문에서 조건이 항상 true이면 프로그램이 멈추지 않습니다.
- 💡 `for...of`는 배열 순회에, `for...in`은 객체 속성 순회에 사용합니다.

---

## 정리

| 개념 | 설명 | 예제 |
|------|------|------|
| `if/else` | 조건 분기 | `if (x > 0) { ... }` |
| `===` | 엄격 비교 | `5 === 5` → `true` |
| 삼항 연산자 | 간단한 조건 표현 | `x > 0 ? "양수" : "음수"` |
| `for` | 횟수 기반 반복 | `for (let i = 0; i < 5; i++)` |
| `for...of` | 배열 요소 순회 | `for (const item of arr)` |
| `while` | 조건 기반 반복 | `while (x > 0) { ... }` |

---

## 다음 단계

- ✅ `exercise.md`의 연습 문제를 풀어보세요.
- 📖 다음 챕터: **ch02-functions-objects: 함수와 객체**
- 🔗 참고 자료: [MDN - 제어 흐름과 에러 처리](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling)
