# 섹션 01: 변수와 데이터 타입

> **난이도**: ⭐ (1/5)
> **선수 지식**: 없음

---

## 학습 목표

이 섹션을 마치면 다음을 할 수 있습니다:

- `let`과 `const`로 변수를 선언할 수 있다
- JavaScript의 기본 데이터 타입을 구분할 수 있다
- `typeof` 연산자로 타입을 확인할 수 있다
- 기본적인 타입 변환을 수행할 수 있다

---

## 핵심 개념

### 변수 선언: let과 const

JavaScript에서 변수를 선언하는 방법은 `let`과 `const` 두 가지입니다.

- **`let`**: 값이 변경될 수 있는 변수를 선언합니다
- **`const`**: 값이 변경되지 않는 상수를 선언합니다

```javascript
// let - 값을 나중에 변경할 수 있음
let age = 25;
age = 26; // 가능

// const - 값을 변경할 수 없음
const name = "김철수";
// name = "이영희"; // 오류 발생!
```

> 💡 **팁**: 기본적으로 `const`를 사용하고, 값이 변경되어야 할 때만 `let`을 사용하세요. 이것은 React에서도 권장되는 패턴입니다.

### 데이터 타입

JavaScript에는 7가지 **원시 타입(primitive type)**이 있습니다:

| 타입 | 설명 | 예시 |
|------|------|------|
| `string` | 문자열 | `"안녕하세요"`, `'hello'` |
| `number` | 숫자 | `42`, `3.14` |
| `boolean` | 참/거짓 | `true`, `false` |
| `undefined` | 값이 없음 (선언만 된 상태) | `undefined` |
| `null` | 의도적으로 비어있음 | `null` |
| `bigint` | 큰 정수 | `9007199254740991n` |
| `symbol` | 고유한 식별자 | `Symbol("id")` |

그리고 **참조 타입(reference type)**으로 `object`가 있습니다 (배열, 함수 포함).

```javascript
const message = "Hello"; // string
const count = 10;        // number
const isActive = true;   // boolean
const data = undefined;  // undefined
const empty = null;      // null
```

### typeof 연산자

`typeof` 연산자로 값의 타입을 확인할 수 있습니다:

```javascript
console.log(typeof "hello");    // "string"
console.log(typeof 42);         // "number"
console.log(typeof true);       // "boolean"
console.log(typeof undefined);  // "undefined"
console.log(typeof null);       // "object" (JavaScript의 유명한 버그!)
```

### 문자열 다루기

문자열은 작은따옴표, 큰따옴표, 또는 **템플릿 리터럴**(백틱)로 만들 수 있습니다:

```javascript
const name = "김철수";
const greeting1 = '안녕하세요';
const greeting2 = `안녕하세요, ${name}님!`; // 템플릿 리터럴 — 변수 삽입 가능

console.log(greeting2); // "안녕하세요, 김철수님!"
```

> 💡 **React 연관**: JSX에서 JavaScript 표현식을 `{}`로 삽입하는 것과 비슷합니다.

### 타입 변환

JavaScript는 타입을 자동으로 변환하기도 합니다 (암시적 변환). 명시적으로 변환할 수도 있습니다:

```javascript
// 문자열 → 숫자
const str = "42";
const num = Number(str); // 42

// 숫자 → 문자열
const n = 100;
const s = String(n); // "100"

// 불리언 변환
Boolean(0);    // false
Boolean("");   // false
Boolean(null); // false
Boolean(1);    // true
Boolean("hi"); // true
```

---

## 코드로 이해하기

### 예제: 변수 선언과 타입 확인

```javascript
// 사용자 정보를 변수에 저장
const userName = "이영희";
let userAge = 28;
const isStudent = false;

// 타입 확인
console.log(`이름: ${userName} (타입: ${typeof userName})`);
console.log(`나이: ${userAge} (타입: ${typeof userAge})`);
console.log(`학생 여부: ${isStudent} (타입: ${typeof isStudent})`);

// 나이 변경 (let이므로 가능)
userAge = 29;
console.log(`생일 후 나이: ${userAge}`);
```

**실행 방법**:
```bash
node exercise.js
```

---

## 주의 사항

- ⚠️ `var`는 오래된 문법입니다. `let`과 `const`를 사용하세요.
- ⚠️ `typeof null`은 `"object"`를 반환합니다. 이것은 JavaScript의 알려진 버그입니다.
- 💡 숫자와 문자열을 `+`로 연결하면 문자열이 됩니다: `"3" + 4` → `"34"`

---

## 정리

| 개념 | 설명 | 예제 |
|------|------|------|
| `let` | 변경 가능한 변수 | `let x = 10;` |
| `const` | 변경 불가능한 상수 | `const PI = 3.14;` |
| 원시 타입 | 기본 데이터 타입 | `string`, `number`, `boolean` |
| `typeof` | 타입 확인 연산자 | `typeof "hi"` → `"string"` |
| 템플릿 리터럴 | 변수 삽입 가능한 문자열 | `` `Hello, ${name}` `` |

---

## 다음 단계

- ✅ `exercise.md`의 연습 문제를 풀어보세요.
- 📖 다음 섹션: **sec02-control-flow: 조건문과 반복문**
- 🔗 참고 자료: [MDN - JavaScript 데이터 타입과 자료구조](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures)
